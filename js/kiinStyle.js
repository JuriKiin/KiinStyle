document.onload = init;

//This init funciton is used to generate certian HTML elements used with the framework.
function init() {
    for(let i = 0; i < document.getElementsByClassName('sticky'); i++) {

    }
}

function ksToggleHam(e) {
    e.classList.toggle("change");
}